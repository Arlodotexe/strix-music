<UserControl
    x:Class="StrixMusic.Shell.ZuneDesktop.Controls.SettingsPages.SettingsBackgrounds"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StrixMusic.Shell.ZuneDesktop.Controls.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="Background" FontSize="20" FontWeight="SemiBold"/>
        <RadioButton Content="Bloom" Style="{StaticResource ZuneRadioButton}"/>
        <RadioButton Content="Bubbles" Style="{StaticResource ZuneRadioButton}"/>
        <RadioButton Content="Cells" Style="{StaticResource ZuneRadioButton}"/>
        <RadioButton Content="Meadow" Style="{StaticResource ZuneRadioButton}"/>
        <RadioButton Content="RobotOwl" Style="{StaticResource ZuneRadioButton}"/>
        <RadioButton Content="Shareds" Style="{StaticResource ZuneRadioButton}"/>
        <RadioButton Content="Wired" Style="{StaticResource ZuneRadioButton}"/>
    </StackPanel>
</UserControl>
