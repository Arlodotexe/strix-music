<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StrixMusic.Shell.Default.Controls"
    xmlns:bindables="using:StrixMusic.ViewModels.Bindables"
    xmlns:converters="using:StrixMusic.Converters"
    x:Class="StrixMusic.Shell.Default.Themes.SearchViewStyle">

    <DataTemplate x:Key="DefaultSearchViewTemplate" x:DataType="bindables:BindableSearchResults">
        <Grid>
            <TextBlock Text="Search results for &quot;Query&quot;"/>
        </Grid>
    </DataTemplate>

    <Style x:Key="DefaultSearchViewControlStyle" TargetType="local:SearchViewControl">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:SearchViewControl">
                        <ContentControl ContentTemplate="{StaticResource DefaultSearchViewTemplate}"
                                        HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style BasedOn="{StaticResource DefaultSearchViewControlStyle}" TargetType="local:SearchViewControl"/>

</ResourceDictionary>