namespace StrixMusic.CoreInterfaces.Enums
{
    /// <summary>
    /// Current state the track is in.
    /// </summary>
    public enum TrackState
    {
        Unloaded,
        Playing,
        Paused,
        Queued,
    }
}
