<UserControl
    x:Class="StrixMusic.Controls.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Microsoft.Toolkit.Uwp.UI"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:StrixMusic.Controls"
    xmlns:musicSources="using:StrixMusic.Controls.MusicSources"
    xmlns:converters="using:StrixMusic.Converters"
    xmlns:settings="using:StrixMusic.Settings"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:FrameworkElementToTagConverter x:Key="FrameworkElementToTagConverter"/>
    </UserControl.Resources>

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PartNavigationView.PaneDisplayMode" Value="Top" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{Binding ElementName=PartNavigationView, Path=CompactModeThresholdWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PartNavigationView.PaneDisplayMode" Value="Auto" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ContentControl Content="{x:Bind AppRoot, Mode=OneWay}" x:Name="AppRootHolder" Visibility="Collapsed" />

        <local:StrixIcon ShowText="True" Margin="10" MaxWidth="300" IsAnimated="True" />

        <muxc:NavigationView Grid.Row="1" x:Name="PartNavigationView"
                             IsBackButtonVisible="Collapsed" IsSettingsVisible="False"
                             PaneDisplayMode="Auto"
                             OpenPaneLength="275"
                             SelectedItem="{Binding ElementName=AppearanceNavigationViewItem, Mode=OneTime}">
            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem Icon="{ui:FontIcon Glyph=&#xE713;}" Content="General" Tag="{x:Bind AppRoot, Mode=OneWay}" />
                <muxc:NavigationViewItem Icon="{ui:FontIcon Glyph=&#xE2B1;}" Content="Appearance" Tag="{x:Bind AppRoot.ShellSettings, Mode=OneWay}" x:Name="AppearanceNavigationViewItem" />
                <muxc:NavigationViewItem Icon="{ui:FontIcon Glyph=&#xEA69;}" Content="Music sources" Tag="{x:Bind AppRoot.MusicSourcesSettings, Mode=OneWay}" />
                <muxc:NavigationViewItem Icon="{ui:BitmapIcon Source=/Assets/Strix/ipfs-logo.png}" Content="IPFS" Tag="{x:Bind AppRoot, Mode=OneWay}" />
                <muxc:NavigationViewItem Icon="{ui:FontIcon Glyph=&#xEE57;}" Content="About" Tag="About" />
                <muxc:NavigationViewItem Icon="{ui:FontIcon Glyph=&#xEBE8;}" Content="Debug" Tag="Debug" />
            </muxc:NavigationView.MenuItems>

            <ScrollViewer MaxWidth="1000">
                <ContentControl Content="{x:Bind PartNavigationView.SelectedItem, Mode=OneWay, Converter={StaticResource FrameworkElementToTagConverter}}"
                                Margin="15"
                                HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <ContentControl.ContentTemplateSelector>
                        <local:SettingsTemplateSelector>
                            <local:SettingsTemplateSelector.MusicSourcesSettingsTemplate>
                                <DataTemplate x:DataType="settings:MusicSourcesSettings">
                                    <musicSources:MusicSourcesSettingsEditor AppRoot="{Binding ElementName=AppRootHolder, Path=Content, Mode=OneWay}"/>
                                </DataTemplate>
                            </local:SettingsTemplateSelector.MusicSourcesSettingsTemplate>

                            <local:SettingsTemplateSelector.ShellSettingsTemplate>
                                <DataTemplate x:DataType="settings:ShellSettings">
                                    <local:ShellSettingsEditor ShellSettings="{x:Bind}" Root="{Binding ElementName=AppRootHolder, Path=Content.StrixDataRoot, Mode=OneWay}" Margin="5" />
                                </DataTemplate>
                            </local:SettingsTemplateSelector.ShellSettingsTemplate>

                            <local:SettingsTemplateSelector.FallbackTemplate>
                                <DataTemplate>
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE822;" FontSize="28"/>
                                </DataTemplate>
                            </local:SettingsTemplateSelector.FallbackTemplate>
                        </local:SettingsTemplateSelector>
                    </ContentControl.ContentTemplateSelector>
                </ContentControl>
            </ScrollViewer>
        </muxc:NavigationView>
    </Grid>
</UserControl>
