<Page
    x:Class="StrixMusic.Pages.Oobe.Welcome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StrixMusic.Pages.Oobe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:StrixMusic.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource OobePageBackgroundColor}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>

        <controls:StrixIcon IsAnimated="True" Height="150" />

        <ProgressBar IsIndeterminate="True" IsEnabled="True" Grid.Row="1" Foreground="#FFAD44E1" />

        <Border Grid.Row="2" />
    </Grid>

</Page>
