namespace StrixMusic.Services.Settings.Enums
{
    /// <summary>
    /// An <see langword=""="enum"/> that indicates the desired shell for the app to use.
    /// </summary>
    public enum PreferredShell
    {
        Default,
        Strix,
        Zune,
    }
}
