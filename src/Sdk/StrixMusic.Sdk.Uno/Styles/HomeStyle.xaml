<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:defaultcontrols="using:StrixMusic.Sdk.Uno.Controls"
    xmlns:sdk="using:StrixMusic.Sdk"
    xmlns:selectors="using:StrixMusic.Sdk.Uno.TemplateSelectors"
    x:Class="StrixMusic.Sdk.Uno.Styles.HomeStyle">

    <DataTemplate x:Key="DefaultHomeViewTemplate" x:DataType="sdk:MainViewModel">
        <UserControl>
            <defaultcontrols:PlayableCollectionGroupPivot DataContext="{x:Bind Library}" RestoreSelectedPivot="True">
                <defaultcontrols:PlayableCollectionGroupPivot.AllEmptyContent>
                    <TextBlock x:Uid="/Common/NothingToShowTB" Text="Nothing to show!" />
                </defaultcontrols:PlayableCollectionGroupPivot.AllEmptyContent>
            </defaultcontrols:PlayableCollectionGroupPivot>
        </UserControl>
    </DataTemplate>

    <Style x:Key="DefaultHomeViewStyle" TargetType="defaultcontrols:HomeView">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="defaultcontrols:HomeView">
                        <ContentControl ContentTemplate="{StaticResource DefaultHomeViewTemplate}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style BasedOn="{StaticResource DefaultHomeViewStyle}" TargetType="defaultcontrols:HomeView"/>

</ResourceDictionary>
