<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:defaultcontrols="using:StrixMusic.Sdk.Uno.Controls"
    xmlns:converters="using:StrixMusic.Sdk.Uno.Converters"
    xmlns:sdk="using:StrixMusic.Sdk"
    x:Class="StrixMusic.Sdk.Uno.Styles.SearchViewStyle">

    <DataTemplate x:Key="DefaultSearchViewTemplate" x:DataType="sdk:MainViewModel">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Search results for &quot;"/>
            <TextBlock Text="Not implemented"/>
            <TextBlock Text="&quot;"/>
        </StackPanel>
    </DataTemplate>

    <Style x:Key="DefaultSearchViewStyle" TargetType="defaultcontrols:SearchView">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="defaultcontrols:SearchView">
                        <ContentControl ContentTemplate="{StaticResource DefaultSearchViewTemplate}"
                                        HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style BasedOn="{StaticResource DefaultSearchViewStyle}" TargetType="defaultcontrols:SearchView"/>

</ResourceDictionary>